export interface TitleAbstract {
  titleAbstractID: number;
  orderNo: string;
  searchDate: string;
  effectiveDate: string;
  client: string;
  clientRefNo: string;
  propertyAddress: string;
  productType: string;
}
